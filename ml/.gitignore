# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/

# Environment / secrets
.env

# Generated pipeline artifacts — regenerable on-demand (see README quickstart).
# Kept for on-demand forecasting: models/best/, data/hourly.csv, archive_*.parquet.
# NB: .gitignore has no inline comments — keep each comment on its own line.
# forecast outputs (incl. latest.csv)
/forecast/
/logs/
# training feature intermediates
/data/interim/
/data/processed/
# 1h-TTL weather cache, re-fetched live
/data/external/openmeteo/forecast_*.parquet
# runtime anchor; rebuilt via --update-state
/models/state.json
